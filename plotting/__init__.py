import os
import sys

# Allow bare imports (e.g. `from plotting_utils import ...`) to work when
# this package is imported from outside the plotting directory.
sys.path.insert(0, os.path.dirname(__file__))
